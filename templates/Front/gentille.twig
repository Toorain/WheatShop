{% extends 'layout.twig' %}

{% block contentHybrid %}
	<div class="container-fluid d-flex justify-content-between flex-wrap" id="hybrid">	
		{% for strain in strains %}
			{% if strain.strain_name == 'Gentille' %}		
			<div class="card col-lg-3 col-md-4 text-white p-0 no-border">
			  <img src="./public/img/{{ strain.img }}" class="card-img w-100 h-100 p-4" alt="{{ strain.name }}">
			  <div class="card-img-overlay m-4">
			    <h5 class="card-title mb-5 bold">{{ strain.name }}</h5>
			    <p class="card-text bold">{{ strain.price }} €</p>
			    <p class="card-text bold">Variety : {{ strain.strain_name }}</p>
			  </div>
			</div>
			{% endif %}
		{% endfor %}	
	</div>
{% endblock %}